auto-install-peers=false
strict-peer-dependencies=false

# Design system da Health Ventures (pacote privado no GitHub Packages).
# O token NAO entra aqui: este arquivo e versionado. Ele fica no ~/.npmrc
# de cada maquina, ou em NODE_AUTH_TOKEN no CI.
@healthventureslm:registry=https://npm.pkg.github.com
